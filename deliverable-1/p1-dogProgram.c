#include <stdio.h>
#include <stdlib.h>

#include "pet_globals.h"
#include "pet_hashtable.h"
#include "pet_input.h"

node table[T_SIZE];
int current = 0;

int main(int argc, char* argv[]){

    printf("%ld\n", sizeof(dogType));
    printf("%ld\n", sizeof(table));
}
